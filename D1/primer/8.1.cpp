#include <iostream>
#include <cmath>
#include <vector>
#include <fstream>

using namespace std;

istream &fx(istream &is)
{
    string s;
    while (is >> s)
        cout << s;
    return is;
}

int main()
{
    return 0;
}
